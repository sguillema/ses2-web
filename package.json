{
  "name": "root",
  "private": true,
  "devDependencies": {
    "extract-text-webpack-plugin": "^4.0.0-beta.0",
    "husky": "^1.3.1",
    "lerna": "^3.13.4"
  },
  "scripts": {
    "update": "lerna bootstrap",
    "dev": "cd packages/application && npm run dev",
    "start": "cd packages/application && npm install && npm run build"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run precommit"
    }
  }
}
